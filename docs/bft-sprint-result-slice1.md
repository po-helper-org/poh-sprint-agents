# БФТ — Навык отчёта по спринту (poh-sprint-agents, slice 2)

> Бизнес-функциональные требования на срез «спринт-ревью». Формат — тот же,
> что у `bft-sprint-planner-slice1.md`: БТ / ПТ / ФТ / НФТ / границы /
> трассировка / якоря. Каждое требование ← источник, НФТ измеримы.

**Статус:** согласован 08.09.2026 (решения PO — §6). **Предмет:** навык `sprint-result`
скрам-мастер-агента. **Функции VISION:** 3 (спринт-ревью) и 5 (презентация).

---

## 0. Контекст (As-Is → Gap → To-Be)

**As-Is.** Навык `sprint-planner` доводит спринт до согласованного ПЛАН и
операционного контракта. На этом контур обрывается. Спринт закрывается вручную:
скрам-мастер вечером перед ревью собирает слайды, переписывая в них строки из
плана и вспоминая по памяти, что из обещанного вышло. Референсная колода
(§ `docs/reference/sprint-report-reference.md`) показывает результат такого
ручного цикла: 8 отчётов за три квартала, устойчивая структура — и в ней же
следы ручной сборки. Проценты готовности расставлены на глаз (`99%` у задачи,
которую откатили; `?` в ячейке результата); у части незакрытых строк нет
причины; строки живут из спринта в спринт, накапливая хвост комментария,
поэтому расхождение с планом накапливается вместе с ними.

**Gap.** Нет навыка, который превращает **обещания ПЛАН + фактический ход
спринта** в отчётный артефакт для бизнеса: с трассировкой каждой строки к
обещанию, с обязательным объяснением незакрытого и с доказуемым вердиктом по
Sprint Goal. Проблема-постановка PO: «текущие скиллы умеют планировать, но не
умеют генерировать отчётную презентацию».

**To-Be.** Навык `sprint-result` (slice 2): приводит любой входящий контекст к
единому формату отчёта. На выходе два артефакта — `ФАКТ-{sprint}.md`
(vault, под git, обратимо) и `ФАКТ-{sprint}.html` (отчётная страница, которую
показывают бизнесу и на которой же собирают правки). Правки со страницы
возвращаются в агент промтом и дают следующую версию. Материализация во
внешнюю презентацию — за границей среза.

---

## 1. Бизнес-требования (БТ)

| ID | Требование | Источник |
|---|---|---|
| **БТ-SR-1** | Агент выдаёт **доказуемый факт спринта**: что команда обещала, что вышло, чем это подтверждается | VISION Func 3; постановка PO |
| **БТ-SR-2** | Отчёт **читается бизнесом без контекста команды** — по нему принимают решение, а не расшифровывают | постановка PO «показать бизнесу что было сделано» |
| **БТ-SR-3** | Каждая строка отчёта **трассируется к обещанию ПЛАН** или явно помечена внеплановой | sprint-planner БТ-SP-2 (симметрия план↔факт) |
| **БТ-SR-4** | **Незакрытое объясняется всегда**: причина и следующий шаг — часть отчёта, а не вопрос с ревью | референс §3 |
| **БТ-SR-5** | **Ноль невыясненных фактов**: каждый процент, число и имя обоснованы источником | VISION принцип 1; sprint-planner БТ-SP-5 |
| **БТ-SR-6** | Отчёт **безопасен к публикации**: уходит наверх и живёт в переписке, поэтому не несёт учётных данных и внутренних ссылок | референс §6 |

## 2. Пользовательские требования (ПТ)

| ID | Роль | Требование | Источник |
|---|---|---|---|
| **ПТ-SR-1** | Бизнес / руководитель | За одну страницу вижу: цель спринта достигнута или нет, чем это доказано, что помешало | БТ-SR-2 |
| **ПТ-SR-2** | PO | Вижу план/факт построчно: что обещали, что вышло, что переносится — без сверки двух документов вручную | БТ-SR-3 |
| **ПТ-SR-3** | PO | Даю дополнительный контекст свободным текстом (что было на самом деле, блокаторы, влёты) и получаю его приведённым к формату отчёта | HowToDemo шаг 2 |
| **ПТ-SR-4** | PO | Правлю готовый отчёт **на самой странице**, не описывая правки словами в чате | HowToDemo шаг 4 |
| **ПТ-SR-5** | Скрам-мастер | Незакрытые строки автоматически становятся кандидатами в carryover следующего спринта | sprint-planner гейт 7 |
| **ПТ-SR-6** | Сотрудник | Мой вклад виден в отчёте так же, как в операционном контракте — по строкам, а не общей массой | sprint-planner ПТ-SP-2 (двойная оптика) |

## 3. Функциональные требования (ФТ)

| ID | Команда | Что делает | Источник |
|---|---|---|---|
| **ФТ-SR-1** | `/sprint-result {sprint}` | Стадия 0 — ресинк факта. Подтягивает `ПЛАН-{sprint}.md`, `operational-contract-{sprint}.md`, ФАКТ предыдущего спринта (для строк, живущих из спринта в спринт). Опрашивает PO по чек-листу **по одному вопросу за раз**: что закрыто, что нет и почему, что прилетело вне плана, что менялось в договорённостях, что показываем на демо. STOP | HowToDemo 1–2; sprint-planner ФТ-SP-1 |
| **ФТ-SR-2** | `/sprint-result {sprint}` | Стадия 1 — сборка. Приводит собранный контекст к эталонной структуре §3.1, прогоняет 8 гейтов (§3.2), пишет `ФАКТ-{sprint}.md` и рендерит `ФАКТ-{sprint}.html`. STOP | HowToDemo 3 |
| **ФТ-SR-3** | (страница) | Отчётная страница несёт **цикл правок**: комментарий к любой строке или выделенному фрагменту, панель открытых правок, сборка правок в промт одной кнопкой | HowToDemo 4; poh-bft-writer `/bft-html` |
| **ФТ-SR-4** | `/sr-revise {sprint}` | Стадия 2 — круг правок. Принимает промт со страницы, вносит правки, **ре-прогоняет гейты**, пишет следующую версию обоих артефактов. Круг фиксируется в «Истории изменений». STOP | HowToDemo 4–5 |
| **ФТ-SR-5** | `/sr-final {sprint}` | Стадия 3 — финализация. Незакрытых правок нет → статус `финальный`, версия фиксируется, страница пересобирается. Отчёт готов к отправке; **отправляет его PO руками** (НФТ-SR-2) | HowToDemo 5 |
| **ФТ-SR-5а** | `/sr-html {путь}` | Сервисная пересборка страницы из `.md` без прогона стадий — после ручной правки файла или падения авто-шага | poh-bft-writer `/bft-html` |
| **ФТ-SR-6** | (вход) | **Cold-start:** нет `ПЛАН-{sprint}.md` → навык не отказывает, а собирает отчёт из одного контекста PO, помечая отсутствие плана один раз в шапке. Трассировка (гейт 1) в этом режиме проверяется против контекста, не против плана | симметрия с cold-start velocity, sprint-planner ФТ-SP-4 |
| **ФТ-SR-7** | (артефакт) | `ФАКТ-{sprint}.md` строго по эталонной структуре — см. §3.1 | референс §2, §5 |
| **ФТ-SR-8** | (артефакт) | `ФАКТ-{sprint}.html` — **формат-мишень**, к которому приводится входящий контекст. Форма — **документ-страница** (прокручиваемый отчёт с выезжающей рейкой), движок и типографика из `poh-bft-writer`. Раскладка проектируется отдельно, после согласования БФТ | HowToDemo 3; решение PO 08.09.2026 |

### 3.1. Структура отчёта (ФТ-SR-7)

Восемь верхнеуровневых блоков фиксированного порядка. Блоков-инициатив может
быть несколько — по числу инициатив, которые спринт двигал. Соответствие
референсу указано в скобках.

1. **Шапка** — спринт, период, ответственный, статус (`✅ Закрыт`), версия.
2. **Итог для бизнеса** — 3–5 строк: что команда показывает по итогам спринта.
   Первое, что читает руководитель; в референсе этого блока нет — он подаётся
   голосом на ревью, из-за чего отчёт не живёт без докладчика.
3. **Блок на каждую ИНИЦИАТИВУ** — KR · **вердикт по Sprint Goal**
   (`достигнут` / `достигнут частично` / `не достигнут`) с доказательством ·
   таблица строк (референс §2). Колонки фиксированного порядка:
   `Задача | Комментарий | Результат | OKR команды`.
   **Инициатива = направление работ = OBJ-срез квартала** — один уровень
   декомпозиции, не три (`decomposition_standard.md`, уровень ИНИЦИАТИВА).
   Заголовок блока — название инициативы, как его знает бизнес.
   `Задача` — с ролевым префиксом `[BE]/[FE]/[SA]/[QA]/[RELEASE]`.
   `Комментарий` — по микро-грамматике референса §3: `Образ результата:`
   (дословно из ПЛАН) · `Факт:` · `Причина:` · `Следующий шаг:` ·
   `БЛОКАТОР:` · `Созависимые:`.
   `Результат` — по шкале референса §4: `100%` / `50–99%` / `0–49%` /
   `ACTIVITY` / `заблокировано`.
4. **Внеплановые работы и влёты** — та же таблица + колонка `Тег`
   (`[UNPLANNED]`/`[TECH-DEBT]`/`[BUG]`/`[SPIKE]`/`[CARRYOVER]`), теги те же,
   что в `sprint_standards.md` (референс §5.1).
5. **Изменения в процессе спринта** — `Затронутые задачи | БЫЛО → СТАЛО | Результат`
   (референс §5.2). Отвечает на вопрос «не успели или поменяли условия».
6. **Процессные метрики** — вложение PO: ссылка на скриншот дашборда +
   **обязательное объяснение текстом** («что изменилось» и «почему»).
   Цифры агент не пересказывает и не выдумывает: источник — скриншот, смысл —
   от PO. Метрика без объяснения не подаётся (референс §5.3). Скриншотов нет →
   блок не пишется.
7. **Демо** — что показываем руками: строка + ссылка на артефакт демонстрации.
8. **Перенос и следующие шаги** — незакрытые строки поимённо как кандидаты в
   carryover + следующие шаги курсивом.

**Симметрия с ПЛАН.** Блоки 3–4 повторяют блоки 2–3 `ideal_sprint_plan.md`
один в один по составу строк: строка ФАКТ рождается из строки ПЛАН, а не
пишется заново. Это делает план/факт машинно-сверяемым (НФТ-SR-3), а не
результатом сличения глазами.

### 3.2. Гейты отчёта (ФТ-SR-2)

Восемь бинарных гейтов по образцу `resources/hard_gates.md`. Светофор тот же:
🔴 при нарушении 1–5 — запись блокируется; 🟡 при 6–8 — запись с явными
`[УТОЧНИТЬ]` под риск PO.

| # | Гейт | Правило |
|---|---|---|
| 1 | Трассировка | Каждая строка ← строка ПЛАН или стоит во «Внеплановых» с тегом. 0 строк-сирот, 0 потерянных строк ПЛАН |
| 2 | Объяснимость | Строка с `Результат` < 100% несёт `Причина:` и `Следующий шаг:` |
| 3 | Вывод процента | Процент **выведен по лестнице готовности** (референс §4.1) из образа результата ПЛАН: названа пройденная ступень, из неё следует число. PO подтверждает или правит на ресинке. Ступень не определяется → `[УТОЧНИТЬ]`, не оценка на глаз |
| 4 | Вердикт | У каждой цели есть вердикт по Sprint Goal с доказательством (что показываем) |
| 5 | Чистота публикации | 0 паролей/токенов/ключей; 0 внутренних URL вне вайтлиста домен-профиля; ФИО — только из ростера |
| 6 | Изменения условий | Договорённости менялись → есть блок 5 (`БЫЛО → СТАЛО → РЕЗУЛЬТАТ`) |
| 7 | Метрика с объяснением | Каждый вложенный скриншот блока 6 несёт объяснение текстом; голых цифр и пересказа графика агентом нет |
| 8 | Перенос назван | Незакрытые строки перечислены поимённо как кандидаты в carryover |

Гейт 5 — блокирующий и в режиме `🟡`: отчёт с учётной записью внутри не
пишется на диск ни при каком решении PO.

## 4. Нефункциональные требования (НФТ) — измеримые

| ID | Требование | Метрика |
|---|---|---|
| **НФТ-SR-1** | Точность | 0 фактов без источника в финальном ФАКТ; у 100% строк процент сопровождён названной ступенью лестницы |
| **НФТ-SR-2** | Обратимость | Всё в vault под git; публикация во внешнюю систему — ручное действие PO, агент не публикует |
| **НФТ-SR-3** | Полнота план/факт | 100% строк ПЛАН имеют строку в ФАКТ: закрыта, перенесена или снята с обоснованием. 0 молча пропавших |
| **НФТ-SR-4** | Объяснимость | 100% строк < 100% несут причину и следующий шаг |
| **НФТ-SR-5** | Читаемость бизнесом | Страница читается без докладчика: блок «Итог для бизнеса» + вердикт по каждой цели. Направлений ≤ 5, строк на направление ≤ 6 (референс: больше не читается) |
| **НФТ-SR-6** | Чистота публикации | 0 учётных данных, 0 внутренних URL вне вайтлиста, 0 ФИО вне ростера — в `.md` и в `.html` |
| **НФТ-SR-7** | Проза | Деловая телеграфная, по `writing_style.md`, без AI-стоп-слов |
| **НФТ-SR-8** | Приёмочная структура | `ФАКТ-{sprint}.md` содержит все 8 блоков §3.1; порядок и колонки не нарушены (структурный валидатор) |
| **НФТ-SR-9** | Цикл правок | Правка со страницы → новая версия за один прогон `/sr-revise`; круг фиксируется, закрытые правки в следующий промт не попадают |
| **НФТ-SR-10** | Автономность страницы | `.html` открывается с диска и работает без сети: правки и их статус живут в браузере |

## 5. Границы системы

**ВНУТРИ slice 2:** ресинк факта, сборка `ФАКТ-{sprint}.md` по эталону,
рендер отчётной страницы, цикл правок «страница → промт → версия», гейты
отчёта, структурный валидатор, симметрия план/факт.

**СНАРУЖИ (следующие срезы):** выгрузка статусов из трекера (сейчас статусы
приходят из ресинка с PO); автоматический сбор процессных метрик (сейчас —
скриншот от PO, О4); слайд-режим страницы (О8); генерация
`.pptx`/Google Slides; daily-каденс как источник факта; ретроспектива и
персональная память; публикация страницы куда-либо, кроме файла на диске.

## 6. Решения PO и оставшиеся вопросы

Согласовано 08.09.2026.

| # | Вопрос | Решение |
|---|---|---|
| **О1** | Единица блока: направление работ (референс) или цель OBJ (ПЛАН)? | **Одна сущность — стратегическая инициатива.** Направление, OBJ-срез квартала и заголовок слайда — один уровень декомпозиции (`decomposition_standard.md`, ИНИЦИАТИВА). Блок = инициатива; симметрия план/факт сохраняется |
| **О2** | Источник процента готовности | **Вывод из образа результата ПЛАН** по лестнице готовности (референс §4.1): агент называет пройденную ступень и следующий из неё процент, PO подтверждает или правит. Гейт 3 проверяет наличие названной ступени, а не самого числа |
| **О3** | Блок «Итог для бизнеса» — в референсе его нет | Добавляем: без него страница не живёт без докладчика (ПТ-SR-1) |
| **О4** | Процессные метрики в slice 2 | **PO прикрепляет скриншоты дашборда отдельно**, агент добавляет объяснение с его слов. Агент не читает графики и не пересказывает цифры. Скриншотов нет → блок не пишется |
| **О5** | Вайтлист внешних ссылок (гейт 5) | Пусто по умолчанию — любая ссылка требует явного разрешения в домен-профиле |
| **О6** | Carryover-мост в следующий спринт | Автоматически: `/sm-sync` уже читает `sprint_fact_doc`, формат совпадёт |
| **О7** | Демо-артефакты: вкладывать или ссылаться | Ссылка + подпись, как и метрики (О4). Вложение файлов — следующий срез |
| **О8** | Форма `.html` | **Документ-страница** (прокручиваемый отчёт, выезжающая рейка, комментарии → промт) — движок `poh-bft-writer`. Слайд-режим для показа на ревью — кандидат в следующий срез |

| **О9** | **Откуда планировщик берёт velocity?** Найдено при стыковке навыков: `sprint-planner` резолвит velocity из ФАКТ прошлых спринтов, но ФАКТ не несёт SP — «закрыто SP» по человеку это персональная оценка, которой в отчёте для бизнеса быть не должно (решение Д3). Планировщик остаётся на fallback `sp_per_person_sprint` и метку `[УТОЧНИТЬ velocity]` не снимает никогда | **Открыт.** Варианты: (а) служебный файл `velocity-{sprint}.md` рядом с ФАКТ — цифры есть, в отчёт не попадают; (б) блок план/факт по ёмкости внутри ФАКТ — противоречит Д3; (в) остаться на fallback и признать, что velocity не резолвится. До решения PO навык ничего не додумывает |

**Остаётся открытым к этапу реализации:** порог «сколько строк на инициативу
до разреза» (НФТ-SR-5 фиксирует ≤ 6 по референсу — проверяется на первом
живом прогоне).

## 7. Заимствования (anti-NIH)

| Актив | Что даёт | Действие в slice 2 |
|---|---|---|
| `sprint-planner/examples/ideal_sprint_plan.md` | Структура ПЛАН (8 блоков) | **Зеркалим** структуру ФАКТ (§3.1), чтобы строки сверялись машинно |
| `sprint-planner/resources/sprint_standards.md` | Теги историй, ролевые префиксы, шкала SP, колонки | **Порт как есть** для внеплановых работ и префиксов |
| `sprint-planner/resources/hard_gates.md` | 8 бинарных гейтов + Светофор 🟢/🟡/🔴 | **Порт механики**, тела гейтов переписаны под факт (§3.2) |
| `sprint-planner/resources/writing_style.md` | Деловая проза без AI-slop | **Порт как есть** (НФТ-SR-7) |
| `sprint-planner/resources/sync_questions.md` | Сценарий ресинка, один вопрос за раз | **Порт каркаса**, вопросы переписаны под факт (ФТ-SR-1) |
| `tests/check_plan_structure.py` | Структурный валидатор markdown-блоков и колонок | **Порт подхода** → `check_report_structure.py` (НФТ-SR-8) |
| `poh-bft-writer/skills/bft-writer/scripts/bft-html-export.{py,css,js}` | Автономная страница ревью: чёрно-белая типографика, выезжающая рейка, комментарии → промт, круги правок по хэшу содержимого | **Порт движка**, разметка переписана под отчёт (ФТ-SR-3, ФТ-SR-8, НФТ-SR-9/10) |
| `poh-bft-writer/commands/bft-html.md` | Контракт сервисной пересборки страницы | **Порт как есть** (ФТ-SR-5) |
| Референсная колода | Грамматика ФАКТ-слайда, микро-грамматика комментария, шкала готовности | **Порт структуры** в обезличенном виде (`docs/reference/`) |

**Дельта (нет ни в одном источнике):** вердикт по Sprint Goal с
доказательством (гейт 4); обязательность «Причина + Следующий шаг» как гейт, а
не привычка (гейт 2); симметрия план/факт как проверяемое свойство (НФТ-SR-3);
гейт чистоты публикации (гейт 5).

## 8. Якоря источников

| Факт | Источник | Ранг |
|---|---|---|
| Постановка задачи, HowToDemo, порядок работ | диалог с PO, 08.09.2026 | R3 |
| Грамматика ФАКТ-слайда, микро-грамматика комментария, шкала готовности | референсная колода → `docs/reference/sprint-report-reference.md` | R2 |
| Структура ПЛАН, теги, префиксы, механика гейтов, стиль | `.claude/skills/sprint-planner/**` | R1 |
| Движок отчётной страницы, цикл «комментарии → промт» | `po-helper-org/poh-bft-writer`, `skills/bft-writer/scripts/bft-html-export.*` | R2 |
| Место среза в контуре (функции 3 и 5), принципы | `VISION.md` | R1 |
| Формат БФТ, планка НФТ | `docs/bft-sprint-planner-slice1.md` | R1 |
