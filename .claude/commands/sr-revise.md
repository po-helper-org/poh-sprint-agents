---
description: "Круг правок: промт со страницы → правки → ре-прогон гейтов → версия +1 + пересборка страницы. STOP."
---
# /sr-revise {sprint}
Роль: Reviser. Вход — промт, собранный на странице отчёта (раздел + строка + текст правки). Прочитай `{sprint_report_doc}`, `resources/fact_gates.md`, `resources/report_structure.md`.

Внеси каждую правку в **тот раздел и ту строку**, которые названы в промте. Правка PO — источник: записывай как есть, не переформулируй. Меняется процент → перепроверь ступень лестницы (гейт 3). Меняется состав строк → перепроверь трассировку к ПЛАН (гейт 1).

Ре-прогони 8 гейтов + `.claude/skills/sprint-result/scripts/check_report_structure.py`. Подними `**Версия:**` на 1, добавь строку в «Историю изменений». **Пересобери страницу** — без этого круг не закрывается и те же замечания придут повторно. Отдай PO путь и список внесённых правок. STOP.
