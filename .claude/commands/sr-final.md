---
description: "Финализация отчёта: оба валидатора, статус «финальный», версия фиксируется, колода пересобирается. STOP."
---
# /sr-final {sprint}
Роль: Finalizer. Прочитай `{sprint_report_doc}`, `resources/fact_gates.md`, `resources/writing_register.md`.

Ре-прогони 9 гейтов. Один 🔴 → не финализировать, назвать гейт.

**Валидаторы — до пересборки колоды, в этом порядке:**
```
python3 .claude/skills/sprint-result/scripts/check_report_structure.py   {sprint_report_doc}
python3 .claude/skills/sprint-result/scripts/sprint-report-style-lint.py {sprint_report_doc}
```
На финализации **оба блокирующие**: гейт 9 здесь 🔴, а не 🟡. Ненулевой код — правь текст документа и прогони снова. Порог линтера не трогать.

Открытые `[УТОЧНИТЬ]` → перечислить PO и спросить, закрываем или финализируем с ними.

Проставь `**Статус:** финальный`, зафиксируй версию, пересобери колоду. Скажи PO, что отчёт готов к отправке, и что **отправляет он сам**: агент наружу не публикует (НФТ-SR-2). Передай список кандидатов в carryover — вход `/sm-sync` следующего спринта. STOP.
